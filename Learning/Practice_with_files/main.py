from pathlib import Path


# Always write next to this script, regardless of current working dir
target_path = Path(__file__).parent / "main.py"

#with open("random_text.txt", mode="r") as file:
 #   text_to_write = "This will go into the doc"
 #   file.read("random_text.txt")

